# Python / uv
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Env / secrets
.env
*.local

# Node / Vite
node_modules/
frontend/dist/

# OS
.DS_Store

# Firebase
.firebase/
firebase-debug*.log

# Private sheet exports — never commit research data (CSV) anywhere in the repo
*.csv

# Eval artifacts (generated traces + results)
backend/evals/artifacts/

# Neo4j dump for migration (data)
*.dump

# Tracked-name leak sensor cache (#104): the live company+alias list pulled for
# the pre-push check. NEVER commit — it is the exact data the sensor exists to
# keep out of the public repo.
.nebula-names-cache

# Local Claude Code state stays out of the public repo, but the codified
# process (custom agents + skills) is versioned like any other code.
.claude/*
!.claude/agents/
!.claude/skills/

# Drift suite report (#108) — regenerated by `make drift`, advisory only.
scripts/drift-report.txt
